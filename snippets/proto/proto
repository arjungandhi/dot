syntax = "proto2";

package cloud.type;

// use the import syntax to use other protos in your proto
import "path/to/proto"

message Type {
    // make everything optional unless its
    // repeated
    optional type field = 1;
    // repeated fields are always optional
    // and are used for dynamically sized arrays
    repeated type field = 2;

}


